<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net5.0-windows</TargetFramework>
    <RuntimeIdentifiers>win10-x64</RuntimeIdentifiers>
    <PublishReadyToRun>true</PublishReadyToRun>
    <Authors>Supernova Dev Team</Authors>
    <Company>Supernova Dev Team</Company>
    <Description>Fast, simple BMS player.</Description>
    <Copyright>(C) 2021 Supernova Dev Team</Copyright>
    <Platforms>x64</Platforms>
    <StartupObject>Supernova.Entrypoint</StartupObject>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <PlatformTarget>x64</PlatformTarget>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
    <PlatformTarget>x64</PlatformTarget>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Core\Main.cs" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="12.0.3" />
    <PackageReference Include="SFML.Net" Version="2.5.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Luminal\Luminal.csproj" />
  </ItemGroup>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Exec Command="robocopy /is /it /njh /njs /nfl /ndl $(SolutionDir)LuminalNAudioEngine\bin\$(ConfigurationName)\net5.0-windows7.0\ $(TargetDir) *.dll /XF Luminal.dll SFML.*.dll&#xD;&#xA;robocopy /njh /njs /nfl /ndl /s /e /r:0 /z $(SolutionDir)SupernovaResources $(TargetDir)&#xD;&#xA;exit /b 0" />
  </Target>

</Project>
